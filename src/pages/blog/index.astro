---
import ListLayout from '~/layouts/ListLayout.astro';
import { usePosts, usePostsCount } from '~/lib/content';

const post = await usePosts();
const total = await usePostsCount();
---

<ListLayout
  posts={post}
  page={{
    total: Math.ceil(total / 5),
    current: 1
  }}
/>
