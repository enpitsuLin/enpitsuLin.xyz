---
import ListLayout from '~/layouts/ListLayout.astro';
import { usePosts, usePostsCount } from '~/lib/sanity';

const post = await usePosts({ limit: 5 });
const total = await usePostsCount();
---

<ListLayout
  posts={post}
  page={{
    total: Math.ceil(total / 5),
    current: 1
  }}
/>
